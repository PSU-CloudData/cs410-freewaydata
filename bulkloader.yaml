# Autogenerated bulkloader.yaml file.
# You must edit this file before using it. TODO: Remove this line when done.
# At a minimum address the items marked with TODO:
#  * Fill in connector and connector_options
#  * Review the property_map.
#    - Ensure the 'external_name' matches the name of your CSV column,
#      XML tag, etc.
#    - Check that __key__ property is what you want. Its value will become
#      the key name on import, and on export the value will be the Key
#      object.  If you would like automatic key generation on import and
#      omitting the key on export, you can remove the entire __key__
#      property from the property map.

# If you have module(s) with your model classes, add them here. Also
# change the kind properties to model_class.
python_preamble:
- import: base64
- import: re
- import: google.appengine.ext.bulkload.transform
- import: google.appengine.ext.bulkload.bulkloader_wizard
- import: google.appengine.ext.db
- import: google.appengine.api.datastore
- import: google.appengine.api.users

transformers:

- kind: Detector
  connector: csv
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: detectorclass
      external_name: detectorclass
      # Type: Integer Stats: 69 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: detectorid
      external_name: detectorid
      # Type: Integer Stats: 69 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: highwayid
      external_name: highwayid
      # Type: Integer Stats: 69 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: lanenumber
      external_name: lanenumber
      # Type: Integer Stats: 69 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: locationtext
      external_name: locationtext
      # Type: String Stats: 69 properties of this type in this kind.

    - property: milepost
      external_name: milepost
      # Type: Float Stats: 69 properties of this type in this kind.
      import_transform: transform.none_if_empty(float)

    - property: stationid
      external_name: stationid
      # Type: Integer Stats: 69 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)


- kind: Highway
  connector: csv
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: direction
      external_name: direction
      # Type: String Stats: 12 properties of this type in this kind.

    - property: highwayid
      external_name: highwayid
      # Type: Integer Stats: 12 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: highwayname
      external_name: highwayname
      # Type: String Stats: 12 properties of this type in this kind.

    - property: shortdirection
      external_name: shortdirection
      # Type: String Stats: 12 properties of this type in this kind.


- kind: LoopData
  connector: csv
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: detectorid
      external_name: detectorid
      # Type: Integer Stats: 99 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: dqflags
      external_name: dqflags
      # Type: Integer Stats: 99 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: occupancy
      external_name: occupancy
      # Type: Integer Stats: 99 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: speed
      external_name: speed
      # Type: Integer Stats: 99 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: starttime
      external_name: starttime
      # Type: Date/Time Stats: 99 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%d %H:%M:%S-07')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: status
      external_name: status
      # Type: Integer Stats: 99 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: volume
      external_name: volume
      # Type: Integer Stats: 99 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)


- kind: Station
  connector: csv
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: downstream
      external_name: downstream
      # Type: Integer Stats: 34 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: highwayid
      external_name: highwayid
      # Type: Integer Stats: 34 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: latlon
      external_name: latlon
      # Type: GeoPt Stats: 34 properties of this type in this kind.

    - property: locationtext
      external_name: locationtext
      # Type: STRING Stats: 0 properties of this type in this kind.

    - property: milepost
      external_name: milepost
      # Type: Float Stats: 34 properties of this type in this kind.
      import_transform: transform.none_if_empty(float)

    - property: numberlanes
      external_name: numberlanes
      # Type: Integer Stats: 34 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: stationclass
      external_name: stationclass
      # Type: Integer Stats: 34 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: stationid
      external_name: stationid
      # Type: Integer Stats: 34 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: upstream
      external_name: upstream
      # Type: Integer Stats: 34 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)
